# Core Service Environment Configuration
# Copy this file to .env for local development

# Database path
DEVICEHUB_DB=/var/lib/edgeberry/devicehub/devicehub.db

# HTTP server port
PORT=8080

# Admin credentials (CHANGE THESE IN PRODUCTION!)
ADMIN_USER=admin
ADMIN_PASSWORD=change-me-in-production

# JWT session configuration
JWT_SECRET=dev-change-me
# JWT session timeout in seconds (default: 86400 = 24 hours)
# Examples:
#   - 1 hour: 3600
#   - 8 hours: 28800
#   - 24 hours: 86400 (default)
#   - 7 days: 604800
JWT_TTL_SECONDS=86400

# Certificates directory
CERTS_DIR=/var/lib/edgeberry/devicehub/certs

# MQTT broker URL for provisioning
MQTT_URL=mqtts://localhost:8883

# UI distribution directory (production)
UI_DIST=/opt/Edgeberry/devicehub/ui/build
