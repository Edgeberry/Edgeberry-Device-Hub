/**
 * Admin Page (placeholder)
 *
 * Purpose: Reserved for future admin-only content.
 * Auth: Route is protected by `RequireAuth` in `App.tsx`.
 */
export default function Admin(props:{user:any}){ return <div>Admin</div>; }
